# 代码与界面审查 · 2026-08-31

本轮同时修改界面和已复现的可靠性问题。范围包括工具栏弹窗、页面悬浮窗、内容提取、图片请求、跨入口状态和发布校验；未增加生产依赖或扩展权限；本批纳入 v1.8.0 GitHub 发布。

## 界面判断与已实施调整

原界面的模板感主要来自具体的叠加：300px 面板中同时出现渐变底、玻璃模糊、内描边、多层圆角卡片、彩色状态胶囊和发光主按钮；下载、复制、刷新在同一行争夺注意力。操作结束还同时出现面板结果和页面角落提示。工具本身只有格式选择与导出，装饰占了过多空间。

本轮改动：

- 统一 `ui-tokens.css`，两入口共用系统明暗主题、字体层级和中性色。状态颜色只用于确有含义的反馈。
- 移除渐变、模糊、发光、呼吸动画与卡片套卡片；悬浮按钮缩为 40px，面板采用细分隔线和小圆角。
- 保留“格式 → 下载 / 复制”的操作顺序，把刷新、诊断和版本移至底部。
- 明确说明复制始终使用图片链接；不再让 ZIP / 内嵌选择暗示剪贴板格式也会变化。
- 面板展开时只显示就地结果；收起时显示简短通知。错误和部分导出警告保留到下一次操作。
- 面板独立定位，窄窗口优先放在按钮上方或下方；长内容可滚动，不再为凑空间将面板压到 180px。
- 关闭时设置 `inert`，Escape 恢复到悬浮按钮，保留可见键盘焦点和减少动画设置。`inert` 会使子元素退出键盘焦点及可访问树。[MDN 文档](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes/inert)

## 已复现并修复的问题

| 级别 | 问题与复现条件 | 修复位置与结果 |
| --- | --- | --- |
| P1 | 状态 ID 子串相似，或其他帖子引用当前目标时，`getMainTweet()` 可能导出错误帖子 | `content-core.js`：完整解析 ID，仅用帖子自身的时间链接判定身份，排除嵌套 article 和普通 div 引用。目标未加载时拒绝导出 |
| P1 | 长文提取合并全页富文本，并借用第一条帖子作者；引用长文也可能触发错误就绪状态 | `getArticleContext()` 统一正文范围和所属作者；排除引用、编辑器，独立文章出现歧义时拒绝拼接 |
| P1 | 头像链接被识别为外链卡片，正文和文件标题出现 `x.com x.com` | 排除作者与头像区域、个人主页链接；UI 的外链标签复用同一识别函数 |
| P1 | Clipboard API 等待期间点击取消，完成后的取消异常被 catch 吞掉，随后又执行备用复制 | `content.js`：取消检查不进入 fallback，备用复制前再次检查信号，并恢复原焦点 |
| P2 | 弹窗关闭再打开丢失正在执行的任务；其他标签的进度串入当前弹窗；旧任务的延迟消息可能覆盖新任务 | `PING` 回传任务快照，开始 / 完成广播状态；popup 按 tab、frame 和扩展来源过滤消息，并按协议版本、任务 ID、启动时间和单调修订号拒绝过期状态 |
| P2 | 导出时可改变格式或刷新页面；finally 无条件启用按钮；较晚的 PING / storage 覆盖新状态 | 两入口统一锁定冲突操作，完成后重新检测页面，用状态修订序号和用户选择标记拒绝过期结果 |
| P2 | 先复制诊断、再从另一入口导出，可并发写剪贴板 | 独立诊断锁与导出互斥；两入口识别 `diagnosing`，诊断成功 / 失败均释放锁 |
| P2 | 旧提示定时器提前隐藏新错误；取消按钮在状态刷新后恢复可点击 | 清理旧定时器，持久保存错误；取消状态保持到任务结束 |
| P2 | 页面正文晚于启动后的 1.6 秒出现，已打开面板仍不可用 | 增加合并触发的 DOM 变化检测，忽略扩展自身的 DOM 写入；内容出现后刷新可用状态 |
| P2 | fetch 使用字符串 abort reason 时，取消被误报为普通失败或丢失错误文字 | 后台以 signal 状态和 reason 分类取消、超时。abort reason 可以是任意 JavaScript 值，并不保证是 `AbortError`。[MDN 文档](https://developer.mozilla.org/en-US/docs/Web/API/AbortController/abort) |
| P2 | 没有可信 Content-Length 的图片响应先完整读入内存，再检查 25 MB 上限 | 后台流式累计字节，越界立即取消 reader；同时保留 MIME、域名和超时限制 |
| P2 | 图片 URL 的 `name` 参数排在首位时未升级清晰度 | 前后台均使用 `URL.searchParams`，不再依赖参数顺序 |
| P2 | 发布目录、ZIP、hash 自洽即可通过校验，即使整个制品只有 `unrelated.txt` | `verify-release.js` 从源码白名单生成期望集合，再比较目录与 ZIP 的每个文件字节 |

本批另行扩展了 `PostDocument`：递归保存包裹容器中的标题、段落、引用、代码块、有序列表起点和嵌套列表，并让 Markdown、复制、内嵌图片与 ZIP 渲染共享这套结构。

互动统计也已按语义标签读取原生 `button` 与 `[role="button"]`，支持按钮换序及 `K/M/万/亿` 数量；没有可靠语义标签的旧结构才使用位置回退。

导出消息协议现在带有版本、任务 ID、任务启动时间和单调修订号；弹窗会拒绝旧任务的延迟进度与终态，保留同标签新任务的锁定状态。`embed` / `zip` 的每次导出还共享一个 500 张图片、64 MiB 累计媒体字节预算，达到边界会停止继续读取并返回明确错误；后台同时把跨标签并发图片请求限制为 6 路，额外请求进入 FIFO 可取消队列，最多排队 96 个，并记录队列高水位、完成等待和拒绝计数，标签关闭或开始导航时会清理对应的活动与排队请求。

线程首批已收紧为“同作者 + 明确的 `Replying to` / `Show this thread` 上下文”；缺少关系证据时只导出主帖，悬浮窗标签与实际收集规则共用同一判断。

异步扩展消息继续使用普通监听函数、显式 `return true` 和 `sendResponse`，没有将监听器整体改成会对无关消息隐式返回 Promise 的 async 函数。[Chrome 消息文档](https://developer.chrome.com/docs/extensions/develop/concepts/messaging)

## 2026-09-01 复审修复

- **P1：正文和引用丢失。** 时间戳过滤仅排除时间节点及其链接，不再跳过包含时间戳的普通容器；递归的行内提取也排除元数据。
- **P2：直接图片和带说明图片遗漏。** 行内提取支持识别当前节点，保留容器直属图片及 figure 内的图片；图片继续进入统一文档模型，供复制、链接、内嵌和 ZIP 渲染使用。
- **P2：有序列表缩进。** 按列表标记宽度计算后续行缩进，覆盖编号从 9 到 10 的变化。
- **P2：列表内容重新排序。** 新提取的列表项使用按 DOM 顺序排列的 blocks，段落、子列表、行内尾文和代码块不再分桶；渲染仍兼容旧 inlines/children 结构。
- **P2：代码块空行改写。** 保留代码块内部及末尾换行和空格；仅在普通行内内容中整理多余空行，不再对渲染后的整个文档压缩换行。

新增用例先在修复前确认失败，再验证修复结果。浏览器夹具覆盖包裹时间戳、引用推文、直属图片、图注及交错列表内容；Node 用例覆盖多位编号、旧列表模型兼容和嵌套代码块空白。

## 未完成项与边界

1. **已完成首批，仍有边界：复杂块结构。** 递归文档模型已经保留包裹容器中的标题、段落、引用、代码块，以及有序和嵌套列表；新增的 Node 与真实浏览器 DOM 夹具覆盖这些边界。复杂表格、数学公式、编辑器专用节点和异常混合容器仍未承诺完整保真。
2. **统计首批已完成，仍需真实页面校准。** 当前实现支持原生按钮、语义标签、按钮换序和常见本地化数量格式；当前界面仍默认不导出统计。发布前还要用实时 X 页面核对不同语言和实验版按钮文案。
3. **线程归属仍需真实页面校准。** 当前已拒绝没有关系证据的同作者相邻帖子，但 X 的懒加载、不同语言文案和实验版 social context 仍需夹具覆盖；这不代表完整线程归档。
4. **资源预算仍有边界。** 单任务图片数量、累计媒体字节预算、后台 6 路并发、FIFO 可取消排队（最多 96 个）、队列指标、标签生命周期清理、8 MP canvas 上限、像素缓冲释放和 ZIP 压缩阶段取消检查已经落地，但真实浏览器峰值内存、不同压缩数据的 ZIP 峰值和队列等待分布仍需专门采样。
5. **页面自动检测可继续减轻开销。** 当前观察 DOM 变化并合并检查；大页面仍会重复扫描。下一步应先测量，再按正文范围和路由缓存结果。

以上列为待办，未用“已完成”勾选掩盖。执行顺序与验收标准见 [升级计划](IMPROVEMENT_PLAN.md)。

## 验证方式

2026-09-01 复审后执行 Node 回归：77 项单元测试全部通过；浏览器回归 20 项全部通过（包含真实 DOM 结构、弹窗预览、打包 MV3 扩展安装和服务 Worker 图片请求）。语法检查、发布元数据和可复现打包检查通过；本批纳入 v1.8.0 GitHub 发布；Chrome 商店版本未同步。

- Node 回归覆盖提取边界、消息时序、取消、诊断互斥、图片读取和制品伪造。
- 浏览器测试覆盖真实 MV3 扩展安装 / 注入 / Markdown 下载，以及 fixture 中的复制、ZIP、嵌入图片、取消、键盘焦点、收起提示、320 × 480 边缘拖动、延迟正文和真实 DOM 块结构。
- 界面预览加载实际 HTML / CSS / JS，使用匿名 fixture 数据；预览本身不宣称是已登录 X 的实测结果。
- 发布验证包括语法、单元测试、浏览器测试、元数据检查、可复现打包及逐文件核对。

**限制：未使用用户登录态在实时 X 页面验收；fixture 图片响应与页面结构是受控数据。测试通过不代表已覆盖 X 的所有实验版 DOM、长文类型和 Markdown 编辑器。**
